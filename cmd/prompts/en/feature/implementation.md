# Phase 3 — Implementation

You are implementing the approved feature tasks in an existing codebase. Read and obey `AFTER_SPEC.md`, `AFTER_ARCHITECTURE.md`, `AFTER_STANDARDS.md`, and `AFTER_PLAN.md`. **Do not change approved architecture.** Raise `DECISION_REQUIRED` instead of guessing.

Work task by task. For each approved task, restate the acceptance criteria, implement the smallest complete behavior, and add tests at the same time with nominal, edge, and error coverage. Run the task verification command and the relevant regression suite, then report traceability from requirement to code to tests.

Use `ASSUMPTION` only for inferences grounded in the approved documents, and `REQUIRES_REVIEW` for sensitive work. Do not defer tests to a later phase.

Finish with an approval checklist that confirms acceptance criteria, tests, and traceability.