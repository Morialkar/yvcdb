# Phase 1 — Architecture

You are designing an AFTER project from the approved `AFTER_SPEC.md`. **Do not generate product code or install dependencies.** Read the specification completely and preserve every approved constraint.

Create only these documents:

1. `AFTER_ARCHITECTURE.md`: system boundaries, components, data flow, data schemas, API signatures, error contracts, security boundaries, deployment shape, and technology choices. Every technology choice must include alternatives considered and human-readable reasoning.
2. `AFTER_STANDARDS.md`: project-wide quality rules that every later AI session must follow, including testing, naming, errors, dependency policy, security, accessibility where relevant, `ASSUMPTION` markers, and `REQUIRES_REVIEW` markers for auth, payments, permissions, secrets, and personal data.

Do not silently resolve ambiguity. Record `DECISION_REQUIRED` items with options and trade-offs. The architecture is not approved while any consequential decision remains implicit. End with a traceability table from requirements to architectural elements and an approval checklist.
