# Phase 4 — Implementation

Implement the remaining approved tasks from `AFTER_PLAN.md` in order. Read and obey `AFTER_SPEC.md`, `AFTER_ARCHITECTURE.md`, and `AFTER_STANDARDS.md`. Do not change approved architecture or technology choices; surface a `DECISION_REQUIRED` blocker instead.

For each task:

1. restate its acceptance criteria;
2. implement the smallest complete behavior;
3. add tests at the same time, covering nominal, edge, and error cases;
4. run the task verification command and relevant regression suite;
5. report traceability from requirement to code and tests.

Use `ASSUMPTION:` for any inference not present in the approved documents. Use `REQUIRES_REVIEW:` on auth, payments, permissions, secrets, personal data, or other security-sensitive code. Stop at a human decision rather than guessing.
